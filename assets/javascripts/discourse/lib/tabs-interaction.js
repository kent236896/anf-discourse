/**
 * GitHub Repository Rankings - 选项卡交互脚本
 */
(function () {
  // 确保文档已完全加载
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initTabsInteraction);
  } else {
    initTabsInteraction();
  }

  function initTabsInteraction() {
    console.log("初始化选项卡交互");
    // 这个脚本现在为空，因为我们使用Ember的内置功能处理交互
  }
})();
